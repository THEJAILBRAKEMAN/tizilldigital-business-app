import type {ExpenseItem} from '../../types';import {Input} from './Input';import {Select} from './Select';import {Button} from './Button';
export const EXPENSE_CATEGORIES=['Inventory','Transport','Marketing','Utilities','Salary','Supplies','Tools & Software','Rent','Misc'];
export function ExpenseItemRow({item,onChange,onRemove}:{item:ExpenseItem;onChange:(item:ExpenseItem)=>void;onRemove:()=>void}){return <div className="grid grid-cols-12 gap-1 border border-border bg-surface-2 p-2"><Select className="col-span-3" value={item.category} onChange={e=>onChange({...item,category:e.target.value})}>{EXPENSE_CATEGORIES.map(c=><option key={c}>{c}</option>)}</Select><Input className="col-span-6" value={item.description} onChange={e=>onChange({...item,description:e.target.value})} placeholder="Description"/><Input className="col-span-2" type="number" value={item.amount} onChange={e=>onChange({...item,amount:Number(e.target.value)})}/><Button className="col-span-1 px-1" variant="danger" type="button" onClick={onRemove}>×</Button></div>}
