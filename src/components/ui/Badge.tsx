export function Badge({label}:{label:string}){const key=label.toLowerCase();const bg=key.includes('settled')||key.includes('delivered')||key.includes('synced')?'bg-success':key.includes('failed')||key.includes('void')||key.includes('overdue')?'bg-danger':key.includes('pending')||key.includes('part')?'bg-warn':key.includes('active')?'bg-accent':'bg-text-3';return <span className={`status-stamp ${bg}`}>{label}</span>}
