import type {HTMLAttributes,ReactNode} from 'react';export function Card({title,actions,children,className='',...props}:{title?:string;actions?:ReactNode;children:ReactNode;className?:string}&HTMLAttributes<HTMLElement>){return <section className={`border border-border bg-surface ${className}`} {...props}>{(title||actions)&&<header className="flex items-center justify-between border-b border-border bg-surface-2 px-3 py-2"><h2 className="font-mono text-xs font-bold uppercase tracking-widest">{title}</h2>{actions}</header>}<div className="p-3">{children}</div></section>}
