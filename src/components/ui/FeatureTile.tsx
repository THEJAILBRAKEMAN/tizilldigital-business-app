import type {LucideIcon} from 'lucide-react';import {useLocation,useNavigate} from 'react-router-dom';
export function FeatureTile({icon:Icon,label,route,badge,onAction}:{icon:LucideIcon;label:string;route?:string;badge?:number|string;onAction?:()=>void}){const nav=useNavigate();const loc=useLocation();const active=route&&loc.pathname===route.split('?')[0];return <button type="button" onClick={()=>onAction?onAction():route&&nav(route)} className={`relative flex min-h-16 flex-col items-center justify-center border border-border bg-surface p-2 hover:bg-surface-2 md:min-h-24 ${active?'border-t-2 border-t-accent':''}`}><Icon className="mb-2 h-6 w-6 text-text-2 group-hover:text-accent"/><span className="font-mono text-[11px] font-bold uppercase tracking-wider text-text-2">{label}</span>{badge!==undefined&&Number(badge)>0&&<span className="absolute right-1 top-1 bg-accent px-1 font-mono text-[10px] font-bold text-white">{badge}</span>}</button>}
