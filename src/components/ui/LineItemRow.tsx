import type {LineItem} from '../../types';import {Input} from './Input';import {Button} from './Button';
export function LineItemRow({item,onChange,onRemove}:{item:LineItem;onChange:(item:LineItem)=>void;onRemove:()=>void}){return <div className="grid grid-cols-12 gap-1 border border-border bg-surface-2 p-2"><Input className="col-span-3" value={item.code} onChange={e=>onChange({...item,code:e.target.value})} placeholder="Code"/><Input className="col-span-4" value={item.description} onChange={e=>onChange({...item,description:e.target.value})} placeholder="Description"/><Input className="col-span-2" type="number" min={1} value={item.qty} onChange={e=>onChange({...item,qty:Number(e.target.value)})}/><Input className="col-span-2" type="number" value={item.unitPrice} onChange={e=>onChange({...item,unitPrice:Number(e.target.value)})}/><Button className="col-span-1 px-1" variant="danger" onClick={onRemove} type="button">×</Button></div>}
