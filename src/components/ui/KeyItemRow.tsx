import {useState} from 'react';import type {KeyItem} from '../../types';import {Input} from './Input';import {Button} from './Button';
export function KeyItemRow({item,onChange,onRemove}:{item:KeyItem;onChange:(item:KeyItem)=>void;onRemove:()=>void}){const [show,setShow]=useState(false);return <div className="grid grid-cols-12 gap-1 border border-border bg-surface-2 p-2"><Input className="col-span-3" value={item.productCode} onChange={e=>onChange({...item,productCode:e.target.value})} placeholder="Code"/><Input className="col-span-3" value={item.productName} onChange={e=>onChange({...item,productName:e.target.value})} placeholder="Product"/><Input className="col-span-3" value={show?item.productKey:'•'.repeat(item.productKey.length||8)} onChange={e=>onChange({...item,productKey:e.target.value})} placeholder="Key"/><Button className="col-span-1 px-1" variant="secondary" type="button" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</Button><Button className="col-span-1 px-1" variant="secondary" type="button" onClick={()=>navigator.clipboard.writeText(item.productKey)}>Copy</Button><Button className="col-span-1 px-1" variant="danger" type="button" onClick={onRemove}>×</Button></div>}
