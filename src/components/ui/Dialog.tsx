import type {ReactNode} from 'react';import {useLayoutMode} from '../../store/useLayout';import {Button} from './Button';
export function Dialog({open,title,children,onClose}:{open:boolean;title:string;children:ReactNode;onClose:()=>void}){const mode=useLayoutMode();if(!open)return null;return <div className="fixed inset-0 z-50 flex items-end bg-black/50 lg:items-center lg:justify-center"><div className={`w-full border border-border bg-surface ${mode==='desktop'?'max-w-2xl':'max-h-[85vh]'} overflow-auto`}><header className="flex items-center justify-between border-b border-border bg-surface-2 p-3"><h3 className="font-mono text-sm font-bold uppercase tracking-widest">{title}</h3><Button variant="ghost" onClick={onClose}>Close</Button></header><div className="p-3">{children}</div></div></div>}
