import {Link} from 'react-router-dom';import {KeyRound,LayoutDashboard,Receipt,Wallet} from 'lucide-react';
export function MobileTabBar(){const tabs=[['/','Dashboard',LayoutDashboard],['/credit-sales','Sales',Receipt],['/key-delivery','Keys',KeyRound],['/expenditure','Expenses',Wallet]] as const;return <nav className="safe-bottom fixed bottom-0 left-0 right-0 z-40 grid grid-cols-4 border-t border-border bg-surface">{tabs.map(([to,label,Icon])=><Link key={to} to={to} className="flex flex-col items-center border-r border-border px-1 py-2 text-text-2 hover:text-accent"><Icon size={18}/><span className="font-mono text-[10px] uppercase">{label}</span></Link>)}</nav>}
