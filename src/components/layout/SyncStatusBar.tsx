import {useSyncQueue} from '../../store/useSyncQueue';export function SyncStatusBar(){const q=useSyncQueue(s=>s.queue);const last=useSyncQueue(s=>s.lastSyncAt);const pending=q.filter(x=>x.state==='PENDING'||x.state==='FAILED').length;return <div className="border-t border-border bg-surface-2 px-3 py-1 font-mono text-[10px] uppercase tracking-wider text-text-2">Queue: {pending} · Last sync: {last?new Date(last).toLocaleString():'Never'}</div>}
