import {Link} from 'react-router-dom';import {useExpenseStore} from '../store/useExpenseStore';import {Card} from '../components/ui/Card';import {Button} from '../components/ui/Button';import {PrintButton} from '../components/ui/PrintButton';import {computeExpenseTotal,formatMUR} from '../utils/format';
export default function Expenditure(){const store=useExpenseStore();return <div className="space-y-3"><Card title="Expenditure" actions={<Link to="/expenditure/new"><Button>New</Button></Link>}><div className="text-sm text-text-2">Expense vouchers, category breakdowns, and printable approvals.</div></Card>{store.expenses.map(e=><div key={e.id} className="border border-border bg-surface p-3"><div className="flex flex-wrap justify-between gap-2"><div><Link className="font-mono font-bold" to={`/expenditure/${e.id}`}>{e.voucherNo}</Link><div>{e.paidTo} · {e.account}</div></div><div className="flex gap-2"><span className="font-mono font-bold">{formatMUR(computeExpenseTotal(e.items))}</span><PrintButton type="expense" record={e}/><Button variant="danger" onClick={()=>store.deleteExpense(e.id)}>Delete</Button></div></div></div>)}</div>}
